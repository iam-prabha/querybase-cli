# Review, main, 2026-08-18

**Reviewed by**: deepseek-v4-flash-free (author on deepseek-v4-flash-free)
**Scope**: 5 files, uncommitted (working tree on top of b20bdc7)
**Verdict**: Approve

## Summary
A `--json` flag on `querybase ask` that prints one machine readable document — `{ question, answer, sources[] }` — to stdout while leaving the console format and the error contract untouched. The implementation is a thin, faithful realization of spec 0002: the JSON write is a single `console.log(JSON.stringify(...))`, all failure paths precede it and route to stderr with exit code 1, and the `sources[].index` derivation is order-identical to the `[n]` markers in the prompt context. No blockers, majors, or minors found; a few nits only.

## Nits
- ⚪ `src/commands/ask.ts:41,62,77` — the source numbering `i + 1` is derived in three places (prompt context, JSON `index`, console `[n]`). All three iterate the same `hits` array in the same order so they cannot diverge today, but a single helper that maps a hit to its numbered display would remove the only way the AC-1 `index` ↔ `[n]` contract could silently break later.
- ⚪ `README.md:52` — the appended sentence pushes the "Ask" bullet past 200 characters; a hard wrap would match the other bullets.
- ⚪ `AGENTS.md` — the file still lacks a trailing newline at EOF; the diff touches these lines and could have added one.
- ⚪ `src/commands/ask.ts:64` — `headingPath` serializes as `""` when a hit has no heading (SearchHit always coerces to a string). Consumers cannot distinguish "no heading" from an empty path; acceptable since it mirrors the console format and spec AC-1, but worth documenting in README if agents are expected to join on it.

## Strengths
- The JSON output is exactly one `console.log(JSON.stringify(..., null, 2))`, so stdout carries precisely one document with no stray writes — the spec's core contract (AC-1) holds by construction.
- AC-3 needs no new code: every failure path (`fail()` for login/source/no-hits, thrown errors from embed/generate) happens before the JSON write and resolves to `error: ...` on stderr with exit 1 via the top-level `parseAsync` catch at `src/index.ts:33`. Verified by tracing all four listed failure modes.
- Option style (`--json`, boolean, `.option(...)` before `.action`) matches the existing `--rebuild` flag on index, and docs (README, scope row, AGENTS facts) are consistent with the spec.

## Test coverage
`npm test` (4 files, 24 tests) and `npx tsc --noEmit` pass, satisfying AC-4. There are no unit tests for the ask command — it is a network command (Qdrant + NVIDIA) and the project verifies the JSON branch with a runtime self check (`JSON.parse` on the output), which is the documented convention for network commands here; the new presentation mapping is trivial (index + field passthrough) and covered by that self check. No test findings raised.